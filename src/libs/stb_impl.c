// this source file only exists to build the stb_image implementation
// (so stb_image isn't rebuilt each time I change texload.cpp)
#define STBI_NO_STDIO
#define STB_IMAGE_IMPLEMENTATION
#include "stb_image.h"
